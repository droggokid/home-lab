# Example environment file for FileBrowser credentials
# Copy this to .env and fill in with actual values
# NEVER commit the actual .env file to git

kubectl -n apps create secret generic filebrowser-auth \
  --from-literal=FB_USERNAME=admin \
  --from-literal=FB_PASSWORD='YOUR_SECURE_PASSWORD_HERE'
