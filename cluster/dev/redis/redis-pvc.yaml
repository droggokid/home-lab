apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: dev
spec:
  storageClassName: local-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
